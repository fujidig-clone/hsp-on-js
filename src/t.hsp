#module mod m_name
#modinit str name
	m_name = name
	return
#modfunc getname
	return m_name
#global

names = "foo", "bar", "", "baz"
repeat 4
	newmod a, mod, names.cnt
loop
delmod a.2

foreach a
	getname a.cnt
	mes ""+cnt+":"+refstr
loop
