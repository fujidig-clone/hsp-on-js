path = "C:\\Program Files\\hsp31\\readme.txt"
mes "all:" + getpath(path, 0)
mes "remove ext: " + getpath(path, 1)
mes "ext only: " + getpath(path, 2)
mes "filename only: " + getpath(path, 8)
mes "lower case: " + getpath(path, 16)
mes "only dir: " + getpath(path, 32)

